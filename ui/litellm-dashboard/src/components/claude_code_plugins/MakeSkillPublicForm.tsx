interface MakeSkillPublicFormProps {
  visible?: boolean;
  onClose?: () => void;
  accessToken?: string;
  skillsList?: any[];
  onSuccess?: () => Promise<void>;
}

export default function MakeSkillPublicForm(props: MakeSkillPublicFormProps) {
  return null;
}
